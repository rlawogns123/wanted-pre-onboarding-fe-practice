import React from 'react';
import styled from 'styled-components';

function Header() {
  return <HeaderText>Angular / Augluar-cli</HeaderText>;
}

export default Header;

const HeaderText = styled.p`
  font-size: 2rem;
  text-align: center;
`;
